apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: "###ZARF_TMPL_NAME###"
  namespace: bigbang
spec:
  targetNamespace: "###ZARF_TMPL_NAMESPACE###"
  interval: 10m
  chart:
    spec:
      chart: "###ZARF_TMPL_PATH###"
      sourceRef:
        kind: GitRepository
        name: "###ZARF_TMPL_NAME###"
  test:
    enable: false
  install:
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: 5
      remediateLastFailure: true
    cleanupOnFail: true
  rollback:
    timeout: 10m
    cleanupOnFail: false
  values:
    istio:
      enabled: "###ZARF_TMPL_ISTIO_ENABLED###"
    monitoring:
      enabled: "###ZARF_TMPL_MONITORING_ENABLED###"
  valuesFrom:
    - kind: Secret
      name: "bigbang-###ZARF_TMPL_NAME###-values"
